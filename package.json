{
  "name": "mappedfair",
  "private": true,
  "version": "0.1.0",
  "workspaces": ["apps/web"],
  "scripts": {
    "dev": "npm run dev --workspace @mappedfair/web",
    "lint": "npm run lint --workspace @mappedfair/web",
    "typecheck": "npm run typecheck --workspace @mappedfair/web",
    "build": "npm run build --workspace @mappedfair/web"
  },
  "engines": {
    "node": ">=24.0.0",
    "npm": ">=11.0.0"
  }
}
