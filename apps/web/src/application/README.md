# Application layer

Place use cases and ports here. Use cases coordinate domain rules and depend on interfaces; infrastructure adapters implement those interfaces.

Keep transport, UI state, and provider-specific logic outside this layer.
