# Feature modules

Create feature folders here only when their MVP acceptance criteria are defined. A feature owns its UI composition and feature-specific application calls; cross-feature policy belongs in the domain or application layer.

Planned candidates: map and routing, accessibility preferences, itinerary, groups, venue data, and concierge. These names do not imply that the features or their data sources are approved.
