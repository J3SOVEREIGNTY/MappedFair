# Domain layer

Place framework-independent entities, value objects, and policies here. This layer must not import React, Firebase, browser storage, map SDKs, BLE APIs, or model-provider clients.

Add a domain module only with a reviewed behavior contract and tests for its invariants.
